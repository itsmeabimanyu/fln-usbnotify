## Requirements

```bash
pip install psutil python-dotenv schedule
```

### Additional Dependencies

- for Linux 

    ```bash
    pip install pyudev
    ```
- for Windows 

    ```bash
    pip install pywin32 wmi
    ```